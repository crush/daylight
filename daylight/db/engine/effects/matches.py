from daylight.db.engine.effects.state import State
import daylight.db.models as models


def establish_match(cursor: postgres.cursor) -> State:
    return []


def unmatch(cursor: postgres.cursor) -> State:
    return []
